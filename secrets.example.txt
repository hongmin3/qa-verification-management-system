# AI Regression Impact Analyzer 비밀정보 파일 (메모장으로 열어서 편집하세요)
#
# 사용법
#   1. 이 파일을 복사해 파일 이름을 secrets.txt 로 바꿉니다.
#   2. 아래 GEMINI_API_KEY= 뒤에 Gemini API Key를 붙여넣습니다.
#   3. 저장한 뒤 서비스를 재시작하거나 화면의 "설정 다시 읽기"를 누릅니다.
#
# 앞뒤 공백, 따옴표, 줄바꿈은 자동으로 정리되므로 그냥 붙여넣어도 됩니다.
# secrets.txt 는 Git에 올라가지 않고, 화면·Report·Log에도 값이 표시되지 않습니다.
# 이 파일을 메신저나 메일로 전달하지 마세요. SSH 비밀번호는 절대 넣지 않습니다.

GEMINI_API_KEY=

# 아래는 선택 사항입니다. 비워두면 기본값을 사용합니다.
GEMINI_MODEL=gemini-2.5-flash

# 서버(10.13.0.222) 운영 자동화용 sudo 비밀번호 (사내망 전용 서버, 선택 사항)
# 이 값은 앱이 읽지 않으며, SSH/sudo 자동화에서만 로컬로 사용됩니다.
SERVER_SUDO_PASSWORD=

# 하위 서비스(매뉴얼 서버) 조회용 전용 계정 — 선택 사항.
# config.yaml 의 services.manual_hub.api_url 과 이 두 값이 모두 있을 때만 연동이 켜진다.
# 매뉴얼 개정 검증의 Cross-Manual 대조에 매뉴얼 서버의 Current 버전을 함께 사용한다.
# 문서를 읽기만 하므로 권한이 가장 낮은 일반 User 계정을 따로 만들어 쓴다 (Admin 금지).
MANUAL_HUB_USER=
MANUAL_HUB_PASSWORD=

# --- 운영 알림 (선택) ---------------------------------------------------------
# Gemini 할당량·결제 문제가 생기면 이 주소로 메일을 보낸다. 비우면 알림이 꺼진다.
# 여러 명에게 보내려면 쉼표로 구분한다.
NOTIFY_EMAIL_TO=
# 발신 SMTP 계정. Gmail 은 2단계 인증을 켠 뒤 "앱 비밀번호"를 발급해 SMTP_PASSWORD 에 넣는다
# (계정 비밀번호로는 SMTP 로그인이 되지 않는다).
SMTP_USER=
SMTP_PASSWORD=
