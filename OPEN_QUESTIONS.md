# Open Questions — 진행 전 확인이 필요한 결정

이 파일에는 **현재 열려 있는(미해결) 결정 사항만** 남긴다. 결정이 나면 그 세션의
`NEXT_STEPS.md` 항목과 커밋에 근거를 남기고 여기서는 지운다 — 지나간 결정 서사를 계속
쌓아두는 대신, 다음에 실제로 판단이 필요한 것만 보이게 하기 위함이다.

## 1. 지식 폴더를 서버에 어떻게 반영할 것인가

제품 지식 폴더는 QA 담당자 PC에 있고 운영 서버에는 없다. 수집 사본(`data/product_knowledge/`)은
Git에 포함되지 않으므로 옮기는 방법을 하나 정해야 한다.

| 방식 | 장점 | 단점 |
|---|---|---|
| A. 담당자 PC에서 CLI 수집 후 결과를 서버로 복사(rsync/scp) | 파싱을 서버가 한 번만 함 | 복사 단계가 수동 |
| B. 서버가 지식 폴더를 네트워크 경로로 마운트 | 서버에서 버튼 하나로 수집 | 마운트 권한·경로 결정 필요 |
| C. `/knowledge` 화면에서 파일을 직접 업로드 | 추가 설정 불필요 | 매주 사람이 반복 |

정해지면 `config/products/*.yaml`의 `knowledge_source.dir`을 그에 맞게 바꾸고
`docs/POST_DEPLOY_TESTS.md` §2에 확정 절차를 적는다. B를 고르면 `${ENV}` 치환으로
서버·PC의 경로를 나눌 수 있다.

## 2. 상위 등급 모델을 쓸 것인가

`gemini-2.5-pro`는 신규 계정에 제공되지 않는다(실측). 권장 대안은 `gemini-3.1-pro-preview`이지만
preview이고 단가가 다르다. 현재 `models.complex`는 검증된 `gemini-2.5-flash`로 두었고,
복잡한 케이스도 Flash로 처리된다.

- 그대로 둔다 → 비용 예측이 쉽고 동작이 검증돼 있다. 복잡한 교차검증 품질은 Flash 수준.
- `gemini-3.1-pro-preview`로 올린다 → 복잡한 케이스 품질 향상 가능. preview 안정성과 단가 확인 필요.

## 3. 사용자 인증을 붙일 것인가 (HTTPS와 함께 결정)

핵심 앱에 로그인이 없어 QA 승인 기록에 사용자가 남지 않는다. 파트원 5명이 함께 쓰면
추적성이 필요해진다. 다만 현재 평문 HTTP로 운영 중이므로, **인증을 붙이면서 HTTPS를 미루면
자격증명이 평문으로 흐른다.** 둘을 함께 결정해야 한다.

- Manual Hub의 세션 인증(Argon2id)을 재사용
- 핵심 앱에 별도 인증
- 사내망 전용이므로 현행 유지

HTTPS는 self-signed로 시도했다가 브라우저 경고 때문에 롤백한 이력이 있다
(`NEXT_STEPS.md` A-0). 정식 CA 또는 사내 CA 배포 방안이 선행되어야 한다.
