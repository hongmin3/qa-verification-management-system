# Open Questions — 진행 전 확인이 필요한 결정

이 파일에는 **현재 열려 있는(미해결) 결정 사항만** 남긴다. 결정이 나면 그 세션의
`NEXT_STEPS.md` 항목과 커밋에 근거를 남기고 여기서는 지운다 — 지나간 결정 서사를 계속
쌓아두는 대신, 다음에 실제로 판단이 필요한 것만 보이게 하기 위함이다.

## 1. 사용자 인증을 붙일 것인가 (HTTPS와 함께 결정)

핵심 앱에 로그인이 없어 QA 승인 기록에 사용자가 남지 않는다. 파트원 5명이 함께 쓰면
추적성이 필요해진다. 다만 현재 평문 HTTP로 운영 중이므로, **인증을 붙이면서 HTTPS를 미루면
자격증명이 평문으로 흐른다.** 둘을 함께 결정해야 한다.

- Manual Hub의 세션 인증(Argon2id)을 재사용
- 핵심 앱에 별도 인증
- 사내망 전용이므로 현행 유지

HTTPS는 self-signed로 시도했다가 브라우저 경고 때문에 롤백한 이력이 있다
(`NEXT_STEPS.md` A-0). 정식 CA 또는 사내 CA 배포 방안이 선행되어야 한다.

## 2. Gemini 유료 사용 사내 승인

결제는 완료됐고 Flash 호출이 실제로 동작한다(2026-09-08 확인). 남은 것은 승인이다 —
사내 문서 조각이 외부로 나가므로 **회사 보안 정책상 외부 생성형 AI API 사용 가능 여부**가
확인되어야 한다. 코드가 아니라 운영 승인 문제다.

무엇이 실제로 나가는지는 분석 결과 화면의 감사 영역에서 원문 그대로 보여줄 수 있다
(전체 코퍼스의 1.37%, 개인정보는 자리표로 마스킹). 승인 요청 시 그 화면을 근거로 쓸 수 있다.

---

## 결정 완료 (2026-09-08)

- **지식 폴더 서버 반영** → 서버가 폴더를 읽기 전용 마운트한다. `${ENV:-기본값}` 문법으로
  서버에서만 환경변수를 설정하고 담당자 PC는 그대로 둔다
  (`docs/POST_DEPLOY_TESTS.md` §2).
- **모델 선택** → `gemini-3.5-flash`를 standard·complex 모두에 쓴다. 같은 Issue·같은
  입력으로 비교해 정했다(`scripts/compare_models.py`, Issue 2건·3회). Pro 계열은 배제 —
  thinking을 끌 수 없고(400), 근거가 하나도 없는데 TC 40건을 전부 KEEP으로 단정했다.
  `gemini-2.5-flash`도 배제 — judgment 필드에 관련도 값을 넣는 스키마 혼동이 재현된다.
- **한글 파일명** → 마운트에서 NFD/mojibake로 와도 앱이 자동 흡수하고, 복구했으면 메모를
  남긴다. fstab 라인은 `scripts/make_knowledge_mount.py`가 생성한다.
- **할당량 소진 알림** → 메일로 보낸다(`app/core/notifier.py`). 수신자는 secrets에 있고
  SMTP 앱 비밀번호만 넣으면 동작한다.
