# Open Questions — 진행 전 확인이 필요한 결정

이 파일에는 **현재 열려 있는(미해결) 결정 사항만** 남긴다. 결정이 나면 그 세션의
`NEXT_STEPS.md` 항목과 커밋에 근거를 남기고 여기서는 지운다 — 지나간 결정 서사를 계속
쌓아두는 대신, 다음에 실제로 판단이 필요한 것만 보이게 하기 위함이다.

## 1. 사용자 인증을 붙일 것인가 (HTTPS와 함께 결정)

핵심 앱에 로그인이 없어 QA 승인 기록에 사용자가 남지 않는다. 파트원 5명이 함께 쓰면
추적성이 필요해진다. 다만 현재 평문 HTTP로 운영 중이므로, **인증을 붙이면서 HTTPS를 미루면
자격증명이 평문으로 흐른다.** 둘을 함께 결정해야 한다.

- Manual Hub의 세션 인증(Argon2id)을 재사용
- 핵심 앱에 별도 인증
- 사내망 전용이므로 현행 유지

HTTPS는 self-signed로 시도했다가 브라우저 경고 때문에 롤백한 이력이 있다
(`NEXT_STEPS.md` A-0). 정식 CA 또는 사내 CA 배포 방안이 선행되어야 한다.

## 2. Gemini 유료 사용 승인

실호출이 선불 크레딧 소진으로 실패한다(`429 RESOURCE_EXHAUSTED`). 사내 문서 조각이
외부로 나가므로 **회사 보안 정책상 외부 생성형 AI API 사용 가능 여부**가 결제와 함께
확인되어야 한다. 코드가 아니라 운영 승인 문제다.

---

## 결정 완료 (2026-09-08)

- **지식 폴더 서버 반영** → 서버가 폴더를 읽기 전용 마운트한다. `${ENV:-기본값}` 문법으로
  서버에서만 환경변수를 설정하고 담당자 PC는 그대로 둔다
  (`docs/POST_DEPLOY_TESTS.md` §2).
- **상위 등급 모델** → `models.complex`를 검증된 `gemini-2.5-flash`로 유지한다.
  `gemini-2.5-pro`는 이 계정에 제공되지 않고(실측 404), 상위 등급이 필요해지면
  `config.yaml` 한 줄로 바꾼다.
