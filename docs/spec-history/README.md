# SPEC 이력

`SPEC.md`는 **현재** 무엇이 맞는가만 담는다. 과거 이력은 두 곳에서 본다.

1. `CHANGELOG.md` — 사람이 읽는 변경 요약. Requirement ID로 연결한다.
2. `git log -p -- SPEC.md` — 문장 단위의 정확한 변경 이력.

이 디렉터리는 기본적으로 비어 있다. 스냅샷(`SPEC-<version>-<date>.md`)은 사양을 크게
다시 쓰면서 **이전 판을 그대로 참조해야 할 때만** 남긴다. 판마다 사본을 쌓으면 어느 것이
현재 사양인지 알 수 없게 된다.
